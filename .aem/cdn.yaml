kind: 'CDN'
version: '1'
data:
  originSelectors:
    rules:
      # Price & Stock Edge Function Route
      - name: price-stock-api
        when: 
          reqProperty: path
          equals: "/api/price-stock"
        action:
          type: selectAemOrigin
          originName: edgefunction-price-stock
          skipCache: true